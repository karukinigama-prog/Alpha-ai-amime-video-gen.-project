name: Alpha AI Anime Generator
on:
  workflow_dispatch:
    inputs:
      user_prompt:
        description: 'Describe the anime scene'
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      - name: Install Tools
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg xvfb libasound2
          pip install ursina requests
          
      - name: AI Code Generation & Rendering
        env:
          # මෙතනට ඔයා Settings වල හැදූ Secret නම හරියටම තියෙන්න ඕනේ
          GITHUB_TOKEN: ${{ secrets.ALPHA_AI_TOKEN }} 
          USER_PROMPT: ${{ github.event.inputs.user_prompt }}
        run: |
          # GPT-4 මගින් කෝඩ් එක ජනනය කිරීම
          python generator.py
          # Ursina මගින් වීඩියෝව Render කිරීම
          xvfb-run -s "-screen 0 1280x720x24" python renderer.py
          
      - name: Upload Anime Video
        uses: actions/upload-artifact@v3
        with:
          name: Alpha-AI-Video
          path: output.mp4
